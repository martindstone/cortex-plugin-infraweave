pipeline:
  identifier: Build_cortex_plugin_infraweave_1754670234927
  name: Build cortex-plugin-infraweave
  orgIdentifier: default
  projectIdentifier: default_project
  properties:
    ci:
      codebase:
        build: <+input>
        connectorRef: ms_gh
        repoName: cortex-plugin-infraweave
  stages:
    - stage:
        identifier: build1
        name: build
        spec:
          caching:
            enabled: true
          cloneCodebase: true
          execution:
            steps:
              - step:
                  type: Run
                  name: print env
                  identifier: Run_3
                  spec:
                    shell: Sh
                    command: |-
                      echo -n "id: "
                      id
                      echo -n "pwd: "
                      pwd
                      echo "home: ${HOME}"
                      . "$HOME/.nvm/nvm.sh"
                      nvm install v22
                      nvm list
              - step:
                  identifier: installdependencies
                  name: install dependencies
                  spec:
                    shell: Sh
                    command: yarn
                  timeout: ""
                  type: Run
              - step:
                  identifier: build
                  name: build
                  spec:
                    shell: Sh
                    command: yarn build
                  timeout: ""
                  type: Run
          platform:
            arch: Amd64
            os: Linux
          runtime:
            spec: {}
            type: Docker
        type: CI
